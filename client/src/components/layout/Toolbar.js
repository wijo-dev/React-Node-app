import React from 'react';

const toolDiv = {
  background: "black",
  height: "50px",
};

class Toolbar extends React.Component {
  render() {
    return(
      <div style={toolDiv}>
        
      </div>
    )
  }
}

export default Toolbar;